[project]
name = "mcp-stdio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.15"
dependencies = [
    "fastmcp>=4.0.3",
]

[project.scripts]
mcp-stdio = "mcp_stdio:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
